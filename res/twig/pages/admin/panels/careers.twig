<div class="tab-pane fade {%if area == 'careers'%} in active {%endif%}" id="tabCareers">
    <form role="form" class="form-horizontal" action="/admin/careers" method="post">
        <legend>Añadir carrera</legend>
        <div class="form-group">
            <label class="col-md-4 control-label" for="career_name">Carrera</label>
            <div class="col-md-4">
                <div class="input-group">
                    <input id="appendedtext" name="career_name" class="form-control" placeholder="Nombre de la carrera" type="text" required="">
                    <div class="input-group-btn">
                        <button class="btn btn-default" type="submit">
                            <i class="glyphicon glyphicon-plus"></i>
                        </button>
                    </div>
                </div>
                <p class="help-block">Nombre de la nueva carrera que quieres añadir</p>
            </div>
        </div>
    </form>
    <hr/>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Carrera</th>
                    <th>Creado por</th>
                    <th>Creado el</th>
                </tr>
            </thead>
            <tbody>
                {% for career in careers_list %}
                    <tr>
                        <td>
                            <form action="/admin/careers" method="post">
                                <button name="delete_career" value="{{ career.getId }}" type="submit" style="font-size: 1em; border:0px solid transparent;" class="glyphicon glyphicon-remove-circle"></button>
                                {{ career.getName }}
                            </form>
                        </td>
                        <td>{{ career.getCreateBy.getFullName }}</td>
                        <td>{{ career.getCreateAt|localizeddate('full', 'none', 'es') }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>